name: Matrix Strategy
on:
#   push:
  workflow_dispatch: 
jobs:
  deploy:
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-22.04]
        image: [hello-world, alpine]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Display Branch
        run: echo ${{ github.ref }}
